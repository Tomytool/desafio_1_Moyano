// area de un cuadrado

function mi_funcion() {
  let lado = parseInt(prompt('Ingrese el valor de A en cm: '));
  let resultado = lado * lado;
  alert('el valor de area de un cuadrado es: ' + resultado + ' cm2');
}
